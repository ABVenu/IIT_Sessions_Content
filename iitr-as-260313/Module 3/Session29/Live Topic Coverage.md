| Topic / Sub-topic | Status | Remarks |
|---|---|---|
| Integrate custom tools into a managed agent executor with explicit iteration limits and parsing-error handling. | Covered | Live e-commerce agent demo: three custom tools registered with `@tool` (`get_order_status`, `estimate_delivery_timeline`, `calculate_refund_amount`); `create_tool_calling_agent` with LLM, tools, and system prompt; `AgentExecutor` wiring with `max_iterations=3` (retry cap on failed loops) and `handle_parsing_errors=True` (demo also toggled to `False` to show crash risk); `agent.invoke({"input": query})` shown end-to-end. |
| Configure transparent step-level observability for tool selection arguments and intermediate observations. | Covered | `return_intermediate_steps=True` used to print each step’s tool name, arguments, and observations; `verbose=True` shown for colored internal execution logs; instructor printed step 1/2/3 for multi-tool runs and explained that setting `return_intermediate_steps=False` hides steps and can break downstream printing; `agent_scratchpad` briefly explained as memory/variable state holder (distinct from intermediate steps). |
| Validate agent behavior across single-tool, multi-tool, and no-tool query classes using the cohort test pack. | Covered | Instructor ran instructor-provided test cases verifying expected tool invocation: single-tool (order 101 status → `get_order_status`), multi-tool (order 102 status + delivery + refund → three tools), and no-tool (flight booking query → no tool call, passes); also live demo queries for delivery timeline (single tool) and combined status/delivery/refund; noted pass/fail is model-dependent (Quen vs Llama); one refund-without-order-ID test discussed as edge case. Term “test pack” not used verbatim—referred to as “test cases” in code. |
| Relate observed traces to expected control flow for each query class without altering instructor-provided scenarios. | Covered | Walked through intermediate-step traces and mapped them to expected flow per query class (direct LLM answer when no tool needed; one tool for single-intent queries; sequential multi-tool chain for compound questions); used printed steps and test pass/fail to confirm tool selection matched expectations; discussed why a refund query might fail depending on model without changing provided scenarios. |
| AI agent components recap — LLM, memory, tools (extra) | Covered (extra) | Opening theory: three pillars of agentic systems (LLM as brain, memory, third-party tools); Gmail/IRCTC integration examples. |
| Manual vs automatic tool calling — comparison with prior session (extra) | Covered (extra) | Contrasted last class (LLM emits tool name/args, developer manually invokes via tool map/`invoke`) with today’s `create_tool_calling_agent` + `AgentExecutor` automatic execution; noted tool map no longer needed. |
| Agent execution flow diagram — query → LLM decision → tool/no-tool path (extra) | Covered (extra) | Whiteboard-style walkthrough: user query → LLM decides tool need → execute tool → feed observation back → final user-friendly answer; motivational-line example for no-tool path. |
| LangChain Classic installation and imports (extra) | Covered (extra) | `pip install langchain-classic` required for agent functions; troubleshooting environment/dependency setup during live coding. |
| E-commerce in-memory order database for demo tools (extra) | Covered (extra) | Hard-coded order records (ORD101 shipped/Delhi, ORD102 cancelled/Bangalore, etc.) used so tool outputs are verifiable during live runs. |
| Model choice impact on agent reliability (extra) | Covered (extra) | Compared Quen vs Llama 3.1 on same queries; noted stronger cloud models would pass more test cases; temperature kept at 0 for deterministic demos. |
| Optional AgentExecutor parameters and defaults (extra) | Covered (extra) | Clarified `handle_parsing_errors`, `agent_scratchpad`, `max_iterations`, `verbose`, and `return_intermediate_steps` are optional with defaults; only agent + executor creation is mandatory. |
| Real-world third-party API integration analogy (extra) | Covered (extra) | Brief mention that production agents would call external APIs (e.g., Amazon refund, Razorpay, delivery partners) instead of in-memory data. |
| End-of-session Mentimeter quiz reinforcing session concepts (extra) | Covered (extra) | Quiz on `create_tool_calling_agent`, `AgentExecutor`, `max_iterations`, `handle_parsing_errors`, intermediate reasoning/tool traces, and prompt placeholders. |
