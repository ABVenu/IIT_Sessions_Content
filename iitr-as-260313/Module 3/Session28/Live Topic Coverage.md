| Topic / Sub-topic | Status | Remarks |
|---|---|---|
| Author LangChain-native tools with typing and descriptions that support reliable model-driven selection. | Covered | `@tool` decorator explained as wrapping Python functions into LangChain tools; live code with `calculate_final_fee`, `check_course_eligibility`, and `get_ticket_status`; tools collected in a list and name-to-function map for lookup; tool name/description printed at runtime; Python return types (`int`, `str`, `None`) briefly explained on functions (~1:33:32–1:34:24). Docstrings as model-facing descriptions were not emphasized as deeply as in lecture notes. |
| Interpret model-emitted structured tool calls and relate them to intended runtime behavior. | Covered | `tool_calls` explained as structured requests inside `AIMessage`; fields covered include tool name, arguments, and `tool_call_id`; `show_tool_calls` demo with sample fee query; for-loop over multiple tool calls in a list; live runs showed correct vs empty vs incorrect tool selection against user intent. |
| Orchestrate a manual tool-feedback loop that arrives at a coherent final model response after tool execution. | Covered | `run_agent_loop` walked through end-to-end: `HumanMessage` → model invoke → read `AIMessage.tool_calls` → execute mapped Python tool → wrap result in `ToolMessage` → invoke model again for final answer; flow diagram drawn on whiteboard/PPT; message history appended so prior tool outputs stay in context. |
| Apply error-containment patterns so tool failures surface as recoverable signals rather than hard crashes. | Not covered | No live coverage of `try/except` around tool execution, structured error JSON, unknown-tool handling, or sending recoverable failure text back via `ToolMessage`. Only incidental mention that small/local models can “fail” unpredictably—not application-level error containment. |
| Diagnose common tool-selection and argument faults using traces from a controlled query set. | Covered | Multiple controlled test queries run (fee, eligibility, ticket status, motivational line, edge cases); instructor inspected printed outputs and `tool_calls` to spot wrong tool choice, wrong discount math, missing ticket ID handling, and empty tool calls; compared Quen vs Llama vs higher temperature to separate model behavior from code bugs. Formal trace-reading workflow (systematic logging/annotation) was not taught as a standalone pattern. |
| Why LLMs need tools vs LLM-only answers (extra) | Covered (extra) | Motivation block: LLMs give general advice but cannot reliably apply exact business rules, live data, or third-party actions without tools; fee/eligibility examples used. |
| Tool-calling flow analogies — restaurant, Zomato, flight booking (extra) | Covered (extra) | Extended analogies mapping user → LLM brain → tool call order slip → tool/kitchen → result → final user reply; flight-booking and payment-tool chain also discussed. |
| Recap of prior manual tool integration vs LangChain approach (extra) | Covered (extra) | Compared previous class pattern (`tools=[...]` passed directly to LLM) with today’s LangChain `@tool` + `bind_tools` workflow. |
| `bind_tools` — attach tools to chat model (extra) | Covered (extra) | Explained as joining/combining Ollama model with registered tools; diagram of LLM component linked to three tools via `bind_tools`; live `model_with_tools = model.bind_tools(tools)`. |
| Message classes — `HumanMessage`, `AIMessage`, `ToolMessage` (extra) | Covered (extra) | Each class role explained in the agent loop; user query wrapped as human message, model reply as AI message, tool output returned as tool message matched by `tool_call_id`. |
| Temperature and model choice affecting tool reliability (extra) | Covered (extra) | Revisited temperature (0 vs 0.3+); live demos showing Quen small model giving wrong math/wrong tool calls vs Llama 3.1/3.2 improving results; noted GPT/Claude would be more reliable for production/financial use cases. |
| Ollama setup, model download, and cohort code run instructions (extra) | Covered (extra) | GitHub code shared; dependencies (`langchain-core`, `.env`, Ollama); `ollama pull` for Llama 3.2 1B / 3.1; learners asked to swap model name and rerun; troubleshooting download size/time. |
| MCP protocol preview (extra) | Covered (extra) | Brief mention that MCP can integrate with tool-calling flows in a later session. |
| RAG / third-party API tools as broader tool ecosystem (extra) | Covered (extra) | Mentioned RAG tools, Skyscanner/flight APIs, Gmail/Slack integrations as examples of external tools an agent can call. |
