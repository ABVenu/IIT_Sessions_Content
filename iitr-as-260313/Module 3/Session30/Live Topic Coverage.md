| Topic / Sub-topic | Status | Remarks |
|---|---|---|
| Extend agent prompts with placeholders that admit rolling conversational history across invocations. | Covered | Introduced `MessagesPlaceholder` for the first time; extended agent prompt with `chat_history` placeholder alongside system prompt, human input, and `agent_scratchpad`; showed manual rolling history via a list with message append on each turn; demonstrated LangChain internal pattern with `RunnableWithMessageHistory`, `get_session_history`, and `InMemoryChatMessageHistory` to persist and inject history across invocations. |
| Demonstrate context continuity across multiple turns where later turns depend on earlier user-supplied facts. | Covered | Live multi-turn e-commerce demo: turn 1 supplies order ID (101/102), turn 2 asks “what is the status of it” and agent resolves using prior context; manual-memory stage (stage 3) and automatic session-based memory (session A) both shown working; instructor explicitly noted “turn 2 depends upon turn 1”; session B contrast shows no cross-session recall when order ID was only given in session A. |
| Diagnose and correct representative defects in history wiring for executor-based agents. | Covered | Diagnosed stateless failure mode by deliberately passing empty `chat_history` each call so follow-up status query cannot resolve; contrasted with manual array wiring where appended messages populate history correctly; explained input-key mismatch defect (`input` vs `human`) in `RunnableWithMessageHistory` config and need for consistent keys; live troubleshooting of import/deprecation issues during automatic-memory run. |
| Justify when message-history attachment changes answer quality versus a stateless agent baseline. | Covered | Side-by-side baseline: stateless agent (memory off, empty history every call) asks user to re-supply order ID on follow-up vs memory-enabled agent correctly answers status from prior turn; opening theory on stateless LLM (each call independent) vs need for memory; end quiz reinforced that stateless agents start every call with empty history. |
| Why AI agents need memory — stateless vs conversational context (extra) | Covered (extra) | Motivational opening: ChatGPT remembers prior questions; without memory an agent cannot answer follow-ups like “what was my first question?” |
| Manual memory with Python list/array (extra) | Covered (extra) | `manual_memory` function stores human and AI messages in a global list, appending after each invoke before passing back into agent. |
| `RunnableWithMessageHistory` wrapper (extra) | Covered (extra) | Explained as internal LangChain function that manages message sequence for executor-based agent; noted deprecation warning and brief LangGraph mention. |
| `InMemoryChatMessageHistory` and session ID isolation (extra) | Covered (extra) | Dictionary-style session storage keyed by session ID; session A retains order context, session B starts fresh; in-memory data lost on program restart vs external DB (e.g., PostgreSQL) for persistence. |
| Prompt wiring — `human` input key, `output_key`, and `agent_scratchpad` (extra) | Covered (extra) | Clarified which config keys map to human messages vs stored output; distinguished `chat_history` (past turns) from `agent_scratchpad` (current-run tool steps). |
| Agent executor recap with e-commerce tool demo (extra) | Covered (extra) | Reused `create_tool_calling_agent` + `AgentExecutor` from prior session with `get_order_status` tool, hard-coded order DB, verbose/parsing-error settings. |
| LangChain Classic imports and live environment fixes (extra) | Covered (extra) | Updated imports from `langchain-classic`; fixed import issues during live run. |
| End-of-session Mentimeter quiz on memory concepts (extra) | Covered (extra) | Quiz on purpose of chat history, `agent_scratchpad` placeholder, optional MessagesPlaceholder flag, manual-memory append order, and stateless agent behavior. |
