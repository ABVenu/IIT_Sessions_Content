| topic/sub topic | status | remarks |
|---|---|---|
| Ingest and segment a document corpus using LangChain loaders and chunking aligned with the established Module 2 corpus design. | covered | Live demo used LangChain `PyPDFDirectoryLoader` and recursive character text splitter with `chunk_size` / `chunk_overlap` on Tesla annual-report PDFs (351 chunks at ~512) and a Nestle/HR PDF (chunk size reduced to 64). Chunk content + metadata (source, page) shown. Corpus was Tesla/Nestle PDFs rather than an explicit Module 2 handbook corpus, but same loader–chunk pattern. |
| Embed and persist vectors in Chroma through LangChain so retrieval remains reproducible across runs. | covered | Embeddings via LangChain Sentence Transformers (GTE-large); Chroma vector DB created in-session for Tesla and Nestle; retriever tested with top-5 similarity. Persist-directory / reload-without-re-embedding for cross-run reproducibility was not clearly demoed (Colab runtime recreated store each run). |
| Construct an LCEL retrieval-augmented answering chain that conditions generation on retrieved passages. | not covered | End-to-end RAG answering was built imperatively: `get_relevant_documents` → join context → system/user prompt with delimiters → LLM API call (Llama 3.3 / temperature 0). No LCEL pipe / Runnable-style chain composition was taught or coded. |
| Critique grounding quality by contrasting outputs with versus without retrieval on representative queries using instructor-supplied comparison criteria. | covered | Grounded generation stressed (answer only from context; say “I don’t know” if missing). Informal with-RAG vs without-RAG / normal-LLM check mentioned in closing; answer correctness checked by reading the PDF (human eval). Formal instructor comparison criteria / RAG triad deferred to later sessions. Mentimeter later asked about `generate without rag` vs RAG answer. |
| Extra: Top-K retrieval and latency–accuracy trade-offs | covered | Extended discussion of choosing K, extremes (K=1 vs K=100), and how raising K increases retrieval and prompt latency. |
| Extra: Prompt delimiters (`###` context / question) | covered | Substantial time on framing user messages so the LLM separates context from question; reinforced in Q&A and quiz. |
| Extra: Grounded-generation system message for RAG | covered | Walked through RAG system prompt (answer only from context; do not mention context; refuse off-corpus questions). |
| Extra: RAG failure modes (chunking, embedding model, metadata, retrieval count) | covered | Theory segment on points of failure before the hands-on pipeline. |
| Extra: Real-world RAG use cases (product manuals scraping, Amazon Rufus-style product Q&A, bank statements) | covered | Post-break discussion of applying the same template beyond Tesla/Nestle. |
| Extra: Multimodal / image embeddings (brief) | covered | Short answer to student question; noted current demo is text-only; multimodal later. |
| Extra: Temperature = 0 for deterministic RAG answers | covered | Called out as important when making the generation call. |
