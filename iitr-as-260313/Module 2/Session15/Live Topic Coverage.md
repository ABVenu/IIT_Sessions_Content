| Topic / detailed sub-topic | Status | Remarks |
| --- | --- | --- |
| Recall Core SQL Operations — Quick recap of CRUD operations and basic SELECT queries from Session 14; bridge into more complex querying | Covered | SELECT/WHERE reviewed; UPDATE and DELETE walked through on sample student data; bridges into ORDER BY, LIMIT, OFFSET, and joins. **Extra:** `LIKE` with wildcards for pattern filtering; keyword case-insensitivity noted. |
| Apply Sorting, Limiting, and Pagination — Use ORDER BY (ASC/DESC), LIMIT, and OFFSET to control result sets; connect this to practical use cases like displaying top results or paginated lists | Covered | ORDER BY with ASC/DESC, LIMIT, OFFSET explained with examples; pagination called out explicitly (including quiz). One online compiler noted as not supporting OFFSET; concept still demonstrated where supported. |
| Understand Database Relationships — Explain the concept of related tables, why data is normalized across multiple tables, and how relationships eliminate redundancy | Covered | Redundant/repeated data example; splitting into smaller tables; normalization defined and motivated. |
| Learn Key Relationship Terminologies — Define one-to-one, one-to-many, many-to-many relationships; primary keys, foreign keys, and referential integrity with real-world examples | Covered | One-to-one, one-to-many, many-to-many and foreign keys explained with examples; junction table in closing quiz. **Not completed vs wording of sub-topic:** “Primary key” and “referential integrity” not introduced as named concepts in the transcript. |
| Design and Create Related Tables in Supabase — Build two related tables (e.g., users and orders) with a foreign key constraint; visualize the schema in Supabase's table editor | Not covered | Supabase mentioned only as optional practice via PPD / next class; no live build of related tables or table-editor walkthrough in this session. |
| Query Relational Data with JOINs — Introduce INNER JOIN using a two-table example (users and orders); write a hands-on query that combines both tables to retrieve meaningful results; understand what a JOIN does conceptually before writing it — limited to two-table joins only | Covered | Concept-first: why combine tables; cross join as Cartesian product then INNER JOIN with ON `customers.customer_id = orders.customer_id`; diagram and row-matching explained. **Extra time:** cross join taught in depth before inner join; borrower/loan banking tie-in. |
| Perform CRUD on Related Data — Insert, update, and delete records across related tables while maintaining referential integrity; understand cascading behavior with simple examples | Not covered | UPDATE/DELETE (and brief insert mention for demo data) shown on a single-table student example, not as coordinated CRUD across related tables; cascading and referential-integrity behavior not addressed in live teaching. |
