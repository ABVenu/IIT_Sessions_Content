| topic/sub topic | status | remarks |
|---|---|---|
| Load and Inspect a Dataset — Open the dataset in Google Colab, explore rows and columns, and identify issues like missing values, duplicates, inconsistent data, and obvious outliers | covered | Notebook-based walkthrough on in-session sample dataframes (car, student, e-commerce); `head`/`display`, issue spotting (NaN, illogical values); shared materials uploaded; Colab referenced only when the live notebook hung; outliers named in the agenda but not treated as a dedicated detection/cleaning step |
| Handle Missing Data — Detect missing values and apply imputation techniques such as mean, median, and mode based on the type of data | covered | NaN meaning; `isnull`/`fillna`; mean, median, and mode with car and student examples; limits of mean imputation; regression-style imputation for offered salary |
| Remove Duplicates and Ensure Consistency — Identify duplicate records and clean inconsistencies or unrealistic values (outliers) to maintain data accuracy | covered | Why duplicates bias models; `drop_duplicates` in recap/Mentimeter; `duplicated()` mentioned in quiz discussion; consistency via fixing impossible values (e.g. negative price with `abs`, invalid grades); outlier handling not a separate technique beyond spotting bad values |
| Prepare Categorical Data — Identify categorical columns and understand the need to convert them into numerical format | covered | `str` categorical columns; models need numeric inputs; ordinal vs nominal distinction |
| Apply Encoding Techniques — Implement label encoding and one-hot encoding on categorical variables and observe how the dataset changes | covered | Label encoding for ordinal data; one-hot/dummy encoding with `pd.get_dummies` on brand and fuel; encoded dataframe inspected |
| Apply Feature Scaling — Normalize and standardize numerical features to ensure uniform scale across all inputs | covered | Feature-scale bias; min–max normalization to 0–1 (formula and manual example); `MinMaxScaler` and `fit_transform` in code; `StandardScaler` named as an alternative; scaling limited to numeric features |
| Prevent Data Leakage in Preprocessing — Apply cleaning steps on training data and reuse them on test data to maintain correct evaluation | covered | Train–test split (e.g. 80/20); fit on training, apply to test; exam-paper analogy; preprocess features (X) only; summary: split before scaling/transformations |
| Evaluate the Impact of Data Preparation — Compare dataset before and after preprocessing to understand improvements in quality and readiness for modelling | covered | Before/after views while fixing inconsistent values and after min–max scaling; end-of-class preprocessing summary; no formal modelling benchmark or structured quality/readiness evaluation |
| *(Extra — not in metadata subtopic list)* Machine learning workflow and recap of supervised learning | covered | Dirty-to-clean data pipeline; prior session tie-in to regression/classification |
| *(Extra — not in metadata subtopic list)* Fixing logically inconsistent values before imputation | covered | Domain-driven fixes on car, student, and e-commerce samples ahead of missing-value work |
| *(Extra — not in metadata subtopic list)* scikit-learn as the upcoming ML stack | covered | `sklearn` preprocessing/scalers; `fit_transform` pattern for later sessions |
| *(Extra — not in metadata subtopic list)* Mentimeter recap quiz | covered | Post-lecture questions on row deletion vs imputation, duplicates, and data leakage |
