| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Load plain-text or PDF documents into a consistent in-memory or file-based corpus. | covered | First demo built an in-memory sample corpus from two plain-text policy files (`returns_policy.txt`, `shipping_policy.txt`); Tesla demo loaded a real 130-page PDF from an unzipped folder using LangChain `PDFDirectoryLoader` with `PyPDF`; instructor also noted loaders for JSON, Excel, and other text formats. |
| Apply chunking strategies with justified chunk size and overlap for a sample dataset. | covered | Theory on fixed-size character splitting with visual demos (512-char chunks, 16-char overlap); discussed extremes (chunk size 1 vs 10,000) and why 512 is a practical starting point; manual `chunk_text` function produced 8 chunks from sample corpus; Tesla demo used `RecursiveCharacterTextSplitter` (chunk_size=512, overlap=16) yielding 351 chunks from the PDF. |
| Attach lightweight metadata (source id; page) to chunks for traceability. | covered | Explained metadata as data-about-data for debugging and evaluation; sample demo attached `source_id`, page number, and chunk index (e.g., `p0_c0`); Tesla chunks showed source file and page number (e.g., page 125) enabling trace-back after retrieval. |
| Persist chunked documents into the vector store created in the prior session. | covered | Sample demo persisted 8 chunks into ChromaDB via `upsert` (ids, documents, metadatas, embeddings) and verified with `peek`; Tesla demo used `Chroma.from_documents` with `persist_directory` and `.as_retriever()`; both reused ChromaDB from the prior session. |
| Extra concept taught: RAG pipeline recap and chunking rationale | covered | Recapped full RAG workflow (load → chunk → embed → store → retrieve → augment → generate); explained why chunking matters vs passing whole documents; discussed top-k retrieval trade-offs (too few vs too many chunks). |
| Extra concept taught: LangChain for document prep orchestration | covered | Introduced LangChain helpers (`RecursiveCharacterTextSplitter`, `PDFDirectoryLoader`, `HuggingFaceEmbeddings`, `Chroma` vector store) as one-line replacements for ~20–30 lines of manual Python. |
| Extra concept taught: Semantic search and retrieval verification | covered | Ran similarity queries against stored chunks in both demos; showed top-k retrieval with distance scores and traced results back to source metadata for debugging. |
| Extra concept taught: End-to-end RAG preview with LLM generation (Tesla demo) | covered | Beyond document prep, demonstrated query → retrieval → Groq LLM grounded answer on Tesla financial report (e.g., annual revenue, VIN-related questions); full generation flow deferred to next session. |
| Extra concept taught: GTE Large embedding model | covered | Tesla demo used GTE Large via Sentence Transformers (contrasted with MiniLM from prior session). |
| Extra concept taught: Advanced chunking and RAG evaluation (forward reference) | covered | Briefly teased semantic chunking and other advanced chunkers for future advanced RAG sessions; mentioned RAG evaluation as a later topic. |
