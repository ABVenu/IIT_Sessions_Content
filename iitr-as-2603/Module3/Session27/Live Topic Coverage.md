| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Demonstrate successful Ollama installation and validation using a laptop-appropriate local model tier. | covered | Ollama download and install walkthrough (ollama.com/download, ~1.3–1.4 GB installer); validation via `ollama version`, system tray/service check, and `ollama list`; live `ollama pull` for DeepSeek R1 (download started but did not finish in session); laptop-appropriate tier emphasized — Qwen 2.5 0.5B (~0.5 GB) used as primary demo model with RAM/GPU sizing guidance (16 GB RAM, no dedicated GPU); `ollama run` explained as next step but not live-demoed (Python API used instead). |
| Implement programmatic chat completion against Ollama from Python. | covered | VS Code demo with OpenAI-compatible client (`client = OpenAI`, localhost base URL/port, `chat.completions.create`, model name from downloaded weights); messages with system/user roles; fast inference shown on Qwen 2.5 0.5B; native `from ollama import chat` shown briefly but instructor preferred OpenAI-compatible pattern for consistency with prior Groq code. |
| Compare local versus Ollama Cloud behavior on an identical prompt using environment-driven configuration. | not covered | Local Ollama and Ollama Cloud were each demonstrated separately in VS Code, and `.env` + `load_dotenv`/`os.getenv` were used for cloud API keys; however, no side-by-side run of the **same prompt on local vs Ollama Cloud** was shown. The timed identical-prompt comparison was **Groq Cloud (70B) vs Ollama Cloud (20B)**, not local vs cloud. No single environment-driven dual-mode script switching between local and cloud endpoints was demonstrated. DeepSeek local performance demo planned but blocked by incomplete download. |
| Apply secure credential handling so secrets stay out of version control while access remains reproducible for the cohort. | covered | `.env` file pattern taught (key=value for `GROQ_API_KEY`, `OLLAMA_API_KEY`); `pip install python-dotenv`, `load_dotenv()`, and `os.getenv()` demonstrated in VS Code; contrasted with hardcoding and Colab Secrets; `.gitignore` for `.env` explained so secrets are not pushed to GitHub while code remains shareable/reproducible for the cohort. |
| Extra concept taught: Paid vs open-source LLMs and when to use each | covered | Extended theory on GPT/Gemini/Anthropic (API-only paid) vs open-source downloadable models; motivation for local LLMs in enterprise (privacy, latency, on-device inference). |
| Extra concept taught: LLM benchmarks and model selection | covered | Substantial time on MMLU, SWE-bench, HellaSwag, TruthfulQA, ARC, reading-comprehension benchmarks; leaderboard sites; how to justify model choice on quality, cost, and benchmark scores. |
| Extra concept taught: Cloud vs local LLM tradeoffs (latency, privacy, cost) | covered | Tesla on-device vs API latency example; bank/VS Code data-leaving-device privacy concern; cloud LLM pros/cons vs local LLM pros/cons diagram discussed before Ollama hands-on. |
| Extra concept taught: Enterprise cloud deployment via Microsoft Azure / Foundry | covered | Azure-hosted GPT models demo; same OpenAI-compatible calling pattern with Azure endpoint and API key; why enterprises prefer cloud over direct OpenAI API. |
| Extra concept taught: Ollama model catalog and sizing heuristics | covered | Tour of ollama.com/models (Qwen, DeepSeek, Gemma, Mistral, GPT-OSS, Nemotron, etc.); rough ~1B params ≈ 1 GB scale; VRAM vs system RAM; smaller models for laptop demos. |
| Extra concept taught: Groq vs Ollama Cloud speed comparison | covered | Same prompt timed on Groq (70B, ~3 s) vs Ollama Cloud (20B, ~10 s); Ollama Cloud free tier and usage limits noted. |
| Extra concept taught: End-of-session poll / Q&A | covered | MCQs on Ollama install validation, `ollama pull`, `.env` security, local vs cloud privacy tradeoff, RAM/GPU model sizing. |
