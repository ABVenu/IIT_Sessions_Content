| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Explain embeddings as semantic coordinates for text and interpret similarity scores. | covered | Embeddings explained as numeric vectors representing semantic meaning; Word2Vec (Google, 2013) history and TensorFlow Projector demo (words as 200-dim vectors in coordinate space); extended to sentence/chunk embeddings via neural network; 2D coordinate plotting demo (C1/C2 close, C3 far) to illustrate proximity = similar meaning; semantic similarity defined as closeness in meaning; Euclidean and cosine distance discussed (cosine preferred); instructor noted individual embedding dimensions are not human-interpretable but collectively encode meaning; Chroma retrieval distances inspected (e.g., 1.17) with Q&A clarifying lower distance = closer semantic match. |
| Create embeddings for sample sentences or document chunks using a standard library. | covered | `sentence-transformers` used throughout: reviewed prior session `embeddings.ipynb` demo; live notebook with `pip install sentence-transformers`, model load, and `model.encode()` on two sample sentences; later re-used same model to encode five document chunks (384-dimensional vectors) before Chroma storage. |
| Store vectors in one vector database (Chroma or FAISS) with minimal configuration. | covered | ChromaDB hands-on demo (FAISS not used): `pip install chromadb`, client creation with `persist_directory`, `get_or_create_collection`, `collection.upsert` with ids/documents/metadatas/embeddings, and `collection.peek` to verify five stored rows; collection compared to SQL table; metadata (category: returns/shipping/account) stored alongside chunks; brief mention of other vector DBs (Pinecone, PGvector) for context. |
| Run a top-k semantic search query and inspect retrieved chunks for relevance. | covered | `collection.query` with query embeddings demonstrated; top-3 retrieval for "return my shoes" query with distance scores and chunk relevance discussed; top-1 retrieval for "forgot password" returned correct account-settings chunk; edge case shown where irrelevant query still returns mathematically closest chunk; instructor emphasized inspecting whether retrieved chunks actually answer the question; `get` vs `query` distinction noted (exact lookup vs similarity search). |
| Extra concept taught: RAG pipeline recap from prior session | covered | Extended recap of full RAG lifecycle—chunking, chunk embeddings, vector DB storage, query embedding, similarity search, retrieve top-k chunks, augment prompt, generate grounded answer. |
| Extra concept taught: Keyword-based search vs semantic search | covered | Contrasted historical keyword/SEO-based search with modern semantic/meaning-based search; linked to how Google search evolved. |
| Extra concept taught: Metadata filtering in vector DB | covered | Metadata explained as data-about-data (e.g., category tags on chunks); discussed filtering/search efficiency benefits before similarity search. |
| Extra concept taught: Vector DB limitation when query is out of domain | covered | Demonstrated that vector DBs always return closest vectors even for completely irrelevant questions—they do not say "I don't know"; tied back to grounding and future RAG sessions. |
