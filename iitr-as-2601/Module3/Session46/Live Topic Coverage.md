| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| Define a JSON schema for an agent response required by the application. | covered | Taught JSON schema as a blueprint for agent output using the e-commerce support-ticket use case (category, priority, summary, needs_human, etc.); live demo creating a Python dictionary and saving it to `schema.json`; explained schema keywords (`type`, `required`, `properties`, enums/constraints); set up `schemas/` and `prompts/` folders; class activity asking students to draft a similar schema for the hospital/medical-notes use case. |
| Prompt the model to return JSON that conforms to the schema. | covered | Showed how to write a strict system message that mirrors the schema shape; created versioned prompt file (`V1_system.txt`); recommended working backwards from schema to prompt text; demonstrated Groq API `response_format={"type": "json_object"}` as an additional way to force JSON syntax mode alongside prompt instructions. |
| Parse model output into Python objects and handle malformed JSON safely. | covered | Taught a three-step parsing pipeline: (1) `strip_markdown_fences` to remove backticks and stray markers, (2) `extract_json_object` to isolate content between first `{` and last `}`, (3) `json.loads` to convert to a Python dictionary; discussed real-world LLM output issues (extra prose, markdown fences, inconsistent formatting); mentioned `safe_parse_model_json` and try/except error-handling patterns for malformed or incomplete JSON. |
| Validate required fields before passing results to tools or UI components. | covered | Implemented `validate_ticket` / `validate_or_raise` to compare parsed output against the saved `schema.json`; checked required keys, field types, and allowed enum values before routing data to the support-ticket UI; used gate/checkpoint analogy—invalid responses should not reach downstream tools or the front end; instructor recapped this as a core session takeaway. |
| Extra: Why agents need structured outputs | covered | Extended motivation: LLM output often feeds databases, Pandas DataFrames, other apps, or other agents; inconsistent formats break inserts and downstream logic. |
| Extra: Medical notes extraction demo (recap) | covered | Revisited open-ended vs schema-enforced prompts; showed problems like "80 KGs", "current smoker", and non-integer values when saving to typed tables. |
| Extra: E-commerce automated support ticket assistant | covered | Primary running example—user query auto-fills ticket fields (category, priority, summary, needs_human) on a dashboard without manual form entry. |
| Extra: Prompt vs schema separation and versioning | covered | Clarified prompts and JSON schemas are separate artifacts; linked to prior session on prompt versioning and folder-based project layout. |
| Extra: JSON fundamentals | covered | Brief primer on JSON as key-value structure and interchangeability with Python dictionaries. |
| Extra: RAG output inconsistency example | covered | Used Tesla/revenue-style answers to show correct content can still fail when output structure varies run to run. |
| Extra: Pydantic (preview) | covered | Mentioned Pydantic as a more robust future approach for schema validation; not implemented in depth this session. |
| Extra: ReAct + JSON schema (Q&A) | covered | Brief discussion on whether ReAct thought/action/observation steps can be structured with JSON schema; noted production focus is usually validating tool inputs and final answers. |
