| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| Configure top-k retrieval against the prepared vector index from previous session that is Embeddings & Vector Search. | covered | Loaded the Tesla vector DB built in the prior session; instantiated a LangChain retriever with `similarity` search and `k=5`; explained trade-offs of low vs high k (speed/noise vs missed info), suggested experimenting with k=3–4 as a balanced default; live demo retrieved top-five chunks for revenue queries against the Chroma index. |
| Assemble retrieved chunks into a prompt context block with clear delimiters. | covered | Built the RAG Q&A pipeline with explicit `#context` and `#question` delimiter tokens in the system message; user message template combines original query plus retrieved context; live walkthrough showed five retrieved chunks assembled into the context block before the LLM API call. |
| Generate answers that cite which chunks supported each claim (informal grounding check). | covered | Demonstrated informal/manual grounding: verified the LLM answer (e.g. $96.77B revenue) against retrieved chunks and traced support to source pages (e.g. page 51, 57); instructor summarized mapping each factual claim to retrieved chunks and metadata for debugging; also taught “say I don’t know if not in context” and temperature=0 for factual grounding (not automated inline citations in model output). |
| Complete a small end-to-end RAG script that ingests; retrieves; and answers. | covered | End-to-end Tesla annual-report demo in Jupyter: load/unzip PDF → chunk (351 chunks) → embed and persist Chroma DB → load retriever → define system/user messages → chat completion → optional Gradio chat UI wrapper; instructor recapped modules (load, chunk, embed/store, retrieve, prompt, answer) and shared code for students to extend. |
| Extra: Full RAG loop recap (chunking → embedding → vector DB → retrieval → generation) | covered | One-slide summary and verbal recap of the complete pipeline, tying today’s session to prior sessions on chunking, embeddings, and ChromaDB. |
| Extra: ChatGPT document upload as in-memory RAG | covered | Live demo showing ChatGPT reading uploaded PDFs, retrieving relevant chunks, and citing sources; explained in-memory vector DB for chat sessions vs enterprise persistent vector stores. |
| Extra: Enterprise RAG vs temporary/session RAG; vector DB sensitivity and update workflow | covered | Discussed massive enterprise vector DBs, customer-facing vs internal admin roles, and manual re-chunk/re-embed when documents change; upsert/update/delete on Chroma mentioned. |
| Extra: Code-repository / Jupyter-notebook RAG use case | covered | Described building RAG on internal company code bases (GitHub Copilot-style) as a practical extension exercise. |
| Extra: Gradio mini RAG app (ChatGPT-style UI) | covered | Wrapped the RAG pipeline in a Python function and exposed it via Gradio (`gr.Interface`) with a public URL for Q&A demo. |
| Extra: Grounding concepts—temperature=0, hallucination prevention, grounding audit quiz | covered | Emphasized grounded answers only from context; live “weather in Delhi” demo returning “I don’t know”; end-of-class quiz on informal grounding audit (map claims to chunks) and grounding illusions (fluent but wrong answers from nearby irrelevant context). |
