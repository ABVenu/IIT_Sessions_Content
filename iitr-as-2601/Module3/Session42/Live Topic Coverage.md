| topic/sub topic | status - (covered/not covered) | remarks |
|---|---|---|
| Distinguish short-term memory from long-term memory | covered | Defined short-term memory as the running chat history for the current session (e.g. ChatGPT chat thread recalling prior turns within one chat); defined long-term memory as cross-session stored summaries/preferences in a vector DB (ChatGPT Memories feature demo); clarified today's lab focuses on short-term memory only. |
| Persist and reload conversation history across turns in a script or notebook | covered | Built conversation history as a Python list of role/content dicts (system → user → assistant); demonstrated in-memory limitation when notebook runtime closes; taught JSON persistence with `json.dump` / `json.load` (save/load history to `basic_history_demo.json`); integrated JSON save/reload into the full Tesla multi-turn RAG notebook. |
| Implement loop termination conditions that prevent runaway agent iterations | covered | Explained infinite chat-loop risk as history grows; taught two stop mechanisms—max steps (hard cap of five turns) and stop words (`quit`, `exit`, `stop`) with `break` inside a `while` loop; live demo showed typing `exit` ends the session with a farewell message and stops further RAG lookups. |
| Handle predictable API or tool errors with user-visible messages | covered | Wrapped the LLM API call (`client.chat.completions.create`) in a Python `try`/`except` block; explained handling token limits, Groq downtime, rate limits, and bad input format without crashing the app; errors are caught and printed as user-visible messages before returning. |
| Extra: RAG recap—single-turn vs multi-turn; Tesla demo continuation | covered | Recapped prior single-turn RAG (no memory, follow-up questions fail) and extended the same Tesla annual-report pipeline to support multi-turn follow-ups (e.g. revenue in 2020 → follow-up "what about 2021?"). |
| Extra: Conversation history message structure and append workflow | covered | Taught maintaining history order (system, user, assistant, user, assistant…); step-by-step manual demo of appending each turn; showed how full conversational context is passed into the RAG prompt on every new query. |
| Extra: Token-limit management via conversation summarization | covered | Discussed token exhaustion in long multi-turn chats; introduced summarization/compression (e.g. summarizing 20–30k tokens down to ~500) and periodic token-counter checks as real-world mitigation strategies. |
| Extra: Full multi-turn memory-integrated RAG code walkthrough | covered | Extended prior RAG Q&A function to initialize history, append user/assistant pairs each turn, combine history + retrieved context + new message, and call the LLM; demonstrated two-turn and multi-turn Tesla Q&A live. |
| Extra: Gradio chat UI for multi-turn RAG demo | covered | Wrapped the memory-enabled RAG pipeline in a Gradio interface for interactive multi-turn Q&A; noted Gradio/Streamlit are for POCs only, not production front-end development. |
