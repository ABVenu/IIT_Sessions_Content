| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Map a multimodal agent pipeline: audio in → text → summary → audio out, and where a simple image → description step fits. | covered | End-to-end pipeline mapped with call-center / voice-assistant framing; three stages explained (STT → LLM summary → TTS); multimodal concept (text, audio, image, video) and embedding-to-numbers foundation covered; vision shown as a parallel input path (image → caption/description) alongside the speech pipeline. |
| Run Speech-to-Text on a short audio sample and check transcript quality. | covered | Live demo on sample campus-update MP3 via Groq Whisper (`whisper-large`); transcript generated and reviewed against the spoken audio; quality-check approach discussed (human review + LLM-as-judge); YouTube auto-captions cited as a real-world STT example. |
| Summarize the transcript with a known cloud model (Groq in notes; Ollama Cloud as the other option to try) into a short useful summary. | covered | Transcript fed to Groq chat completions with a curated summarization prompt; short bullet-point summary produced from the sample audio; Ollama Cloud mentioned as an alternative but Groq used for the live demo; system-prompt customization for summary format discussed. |
| Integrate Text-to-Speech for the summary, run full Speech-to-Text → summary → Text-to-Speech once, and try a lite vision step (image → short description). | partially covered | gTTS integrated and full STT → summary → TTS run once (`spoken_summary.mp3` played in class); lite vision step not demoed live—instructor deferred the image file (“ignore for now”), noted Groq lacks image models, and pointed students to Hugging Face image-captioning models for self-try. |
| Extra concepts taught (beyond listed subtopics) | covered | Multimodal RAG recap with LlamaParse (PDF/image extraction, text vs image chunks, embeddings); Masai/Zoom AI Summary & AI Tutor pipeline (STT → transcript → vector DB → RAG Q&A); YouTube video summarization / Q&A use case; NotebookLM demo (Kunal Shah video → transcript → vector DB → Q&A + audio overview); LLM benchmark-based model selection (MMLU, HumanEval, SWE-bench, etc.); ChatGPT voice mode (STT + TTS, no auto-summarize); customer-support and RAG-with-microphone use cases; PyMuPDF as open-source PDF parsing option; Colab vs local IDE setup guidance. |
