| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Add metadata filters so retrieval respects document type or date tags. | covered | Explained associating metadata (year, company name, file name, product type, status) with chunks; two-layer retrieval (metadata filter first, then similarity search); Amazon customer-care example (product=laptop, status=active); Tesla revenue-by-year (2022) example; compared metadata filtering to a SQL WHERE clause to narrow the search space before vector similarity. |
| Re-chunk a corpus when content changes and re-index with consistent ids. | not covered | Re-chunking and re-indexing when underlying documents are added, updated, or removed was discussed as an ongoing vector-DB maintenance/admin task; no guidance on maintaining consistent chunk IDs during re-index was provided. |
| Tag each chunk with source identifiers for user-visible citations (lite). | covered | Discussed storing source-related metadata on chunks (file name, page number, topic, reference back to the original document in the vector DB); noted prior session covered document-reference metadata during chunking; user-facing citation display in the UI was not demo'd. |
| Apply a grounding checklist before releasing a RAG prompt change. | not covered | "Grounding checklist" was named only in the closing summary; session focused on LLM-as-judge evaluation (groundedness and relevance judges, star ratings, RAG triad including context relevance) rather than a step-by-step pre-release checklist for RAG prompt changes. |
| Extra concepts taught (beyond listed subtopics) | covered | RAG architecture recap (chunking → embeddings → vector DB → retrieval → LLM); fixed-size/recursive text splitter chunking (512 chars, ABB PDF live demo); semantic chunking (concept, cost/latency trade-offs, medium-article walkthrough); ABB/Siemens industry use cases and latency requirements; LLM-as-judge evaluation methodology; groundedness, relevance, and context-relevance (RAG triad) metrics; ChatGPT internal judge + human-evaluator workflow; inference vs indexing time; Gradio RAG demo; Git ignore for persisted vector DB files. |
