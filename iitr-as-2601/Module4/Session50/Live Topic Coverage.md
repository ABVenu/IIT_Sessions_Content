| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Build a small set of questions with expected supporting documents. | covered | Hostel notice-board demo with five chunks (D1–D5); built an evaluation set pairing questions with expected document/chunk IDs (attendance → D1, late submission → D2, library hours → D3, mess refund → D4, Wi‑Fi → D5); added a negative test case (room-rent question with expected answer “none”) to check out-of-corpus behavior. |
| Experiment with top-k and chunk parameters and record retrieval hit rate informally. | not covered | Hit-rate concept explained (e.g., 3/5 chunks matching → 60%); top-k (k=1, 7, 10) and chunk parameters (size, overlap, semantic vs fixed-size) discussed conceptually; static hit/miss results shown for pre-defined questions; no live hands-on experiment varying top-k or chunk settings and recording hit rates. |
| Classify failures as retrieval vs generation and choose the right fix. | partially covered | RAG triad used to separate retrieval issues (context relevance) from generation issues (groundedness, answer relevance); instructor noted retriever is usually the flaw; fixes discussed (semantic chunking, embedding model, top-k) are retrieval-side; no explicit structured workflow to classify a failure as retrieval vs generation and pick the corresponding fix. |
| Demonstrate measurable improvement on at least two failing questions. | not covered | Two failing cases identified (Q2 expected D2 but retrieved D5/D3/D1; Q4 complete miss); improvement strategies described (semantic chunking, embedding swap, k tuning, judge-score comparison); no live before/after demo showing measurable retrieval improvement on those failing questions. |
| Extra concepts taught (beyond listed subtopics) | covered | RAG pipeline recap (chunking → embeddings → vector DB → retrieval → LLM); LLM-as-judge evaluation recap (groundedness, relevance, context relevance); semi-manual / human eval vs automated judge; ranked-list hit evaluation for top-k retrieval; semantic chunking (Medium article walkthrough, merge/split guardrails, cost/latency trade-offs); fixed-size chunking limitations (HR leave-policy example); embedding model selection (GTE-large); hyperparameter-style tuning loop with judge scores; agentic RAG (e-commerce chatbot demo integrating SQL agent + RAG retriever via LangChain tools); agents recap (LLM + tools); multimodal RAG preview for next session. |
