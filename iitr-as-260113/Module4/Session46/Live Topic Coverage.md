| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Explain the role of n8n as a visual automation platform for connecting services and orchestrating tasks. | covered | n8n defined as a visual/no-code workflow automation platform and drag-and-drop automation engine; 1,500+ integrations (Google Sheets, Slack, Gmail, etc.); visual representation of agentic/business workflows; usable by non-technical users with optional Python/JavaScript for custom logic; website walkthrough with HR, sales, and SecOps workflow examples; contrasted with code-only automation and with LLM-only tools. |
| Configure triggers and node connections that move data through a simple multi-step workflow. | not covered | Triggers explained and partially configured (manual, schedule, webhook, app event, form submission); n8n form trigger node built, executed, and form response captured; node types and chaining described conceptually (OpenAI sentiment analysis, Google Sheets row creation). A connected multi-step workflow with data moving between two or more linked nodes was not built live — instructor explicitly treated this as an overview and deferred full multi-step builds to the next two sessions. |
| Apply credentials and environment settings securely for third-party integrations in n8n. | covered | Credential security explained (not stored in plain text; API keys/OAuth); Google Sheets integration used as example; OAuth flow illustrated via third-party login (Google/GitHub); environment variables for self-hosted n8n (`export` on local machine vs cloud UI); Setup Credentials UI walkthrough for Google Sheets access; distinction between self-hosted and n8n-hosted credential handling. |
| Validate a baseline workflow execution with inspectable inputs and outputs at each node. | not covered | Form trigger step executed and outputs inspected at a single node (table, JSON, and schema views; test-mode form submission with name, email, rating); manual trigger workflow executed when empty. No multi-node run was completed, so per-node input/output validation across a baseline multi-step workflow was not demonstrated. |
| Extra concepts taught (beyond listed subtopics) | covered | n8n pricing tiers (starter/pro/business/enterprise) and infrastructure/hosting costs; self-hosted n8n via Docker (volume create, pull image, run container, localhost:5678); Docker basics (image vs container); n8n templates and documentation; expressions for dynamic values (marks-to-grade example); node categories (trigger, logic/if-else, code/Python/LangChain); observability/tracing of agent inputs and outputs; connection to any LLM (OpenAI, Anthropic, Gemini, Ollama); recap link to prior session triggers/events/webhooks; hands-on break for students to install Docker and run n8n locally. |
