| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Design an end-to-end automation that ingests documents or messages and routes them through an LLM summarization step. | covered | Built a full Gmail-triggered n8n pipeline: Gmail "On message received" ingests email (Python code in body), filters by sender, passes content to Basic LLM Chain ("code summarizer") with system/user prompts; later extended with a parallel "code commentator" LLM branch, merge node, and aggregate node before delivery. |
| Integrate delivery mechanisms such as email Slack or database updates as workflow outcomes. | covered | Gmail "Send message" node configured as the workflow outcome—sends aggregated code summary and commented code back to the user. Slack and database delivery mechanisms were not demonstrated. |
| Test the pipeline with representative inputs including one failure and one edge-case path. | not covered | Success-path testing was done extensively (fetch test event, pinned data, full end-to-end execution with a new email). Ad-hoc failures were debugged live (OpenAI subnode/model error, OAuth test-user access error, student snippet-only issue) but no structured failure-path or dedicated edge-case test (e.g., non-code email, empty body, wrong sender) was designed or walked through. |
| Document the workflow for handoff including credentials dependencies and operational assumptions. | covered | Instructor used n8n sticky notes as upfront workflow documentation; detailed Google OAuth (Gmail) and OpenAI API credential setup; operational settings discussed (poll frequency, sender filter, max emails per poll, message-received event). End-of-class workflow exported as JSON, uploaded to GitHub, and import-from-file demonstrated for handoff/reference. |
| Extra: Parallel LLM execution and merge/aggregate pattern | covered | Duplicated LLM chain to run code summarization and code commenting in parallel; explained async/performance rationale; used merge node to wait for both streams and aggregate node to combine text outputs before email send. |
| Extra: Data pinning for iterative testing | covered | Demonstrated pinning Gmail trigger and LLM node outputs to avoid repeated API calls and token consumption during workflow development and testing. |
| Extra: HTML-formatted email output | covered | Optional enhancement—updated LLM prompts to request HTML output, switched Gmail send node from text to HTML type; demo showed improved code readability and formatted summary in received email. |
| Extra: Edit Fields node and manual field mapping | covered | Briefly shown as optional step to separate code-with-comments and code-summary fields before formatting; instructor noted it could be skipped by adjusting earlier LLM prompts instead. |
| Extra: Gmail trigger configuration details | covered | Covered polling interval trade-offs, message-received event, max emails per poll, sender-based filtering, and disabling N8N email attribution. |
| Extra: Workflow JSON export and import | covered | Downloaded workflow as JSON at end of class, uploaded to GitHub repo, and showed import-from-file on n8n for reuse; briefly discussed credential handling on import. |
