| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Distinguish single-agent and multi-agent systems and justify when distributed specialized agents are appropriate. | covered | Single-agent vs multi-agent contrast taught via content-writing use case (research → write → review → edit → publish); single-agent limitations (bloat, debugging difficulty, bottleneck/single point of failure, latency); when to use multi-agent only for complex enough tasks vs single-agent for simple/straightforward work; multi-agent uses multiple specialized agents (research, writing, review, editing). |
| Decompose a complex goal into sub-tasks with clear role ownership and handoff points. | covered | Multi-agent decomposition defined as breaking a complex problem into smaller manageable tasks; extended HR recruitment example (resume review/shortlist → email → schedule interviews → feedback → offer/rejection) with per-agent role ownership; handoff and handoff points defined as passing output/result from one agent to the next; structured JSON handoff example (research agent → writer agent) with fields such as topic, facts, sources, assumptions, confidence, and next-agent metadata. |
| Compare sequential pipelines and collaborative multi-agent workflow patterns using a researcher-writer-editor example. | covered | Sequential multi-agent pipeline explained as agent A → B → C with one-directional flow; researcher-writer pipeline demonstrated with EV-adoption research handoff; review and editing agents named in the content-writing chain. Collaborative workflow contrasted as agents interacting bidirectionally (e.g., writer returning to researcher when confidence is low); orchestrator manages execution order; trade-off noted that collaborative workflows improve quality but consume more time/resources. |
| Explain how HTTP-based APIs enable agents and automation tools to read from and write to external systems. | covered | Why HTTP matters for agentic AI (tool calls, DB calls, agent-to-agent communication over the network); HTTP/HTTPS defined (client-server, request/response); CRUD mapped to HTTP methods (POST create, GET read, PUT update, DELETE delete) with Amazon/sign-up examples; request components (URL, method, body, headers) and response/status codes introduced; agents need HTTP for external actions (email, calendar, tickets, refunds, Google Sheets, etc.). |
| Relate triggers, events, and webhooks to starting and chaining automation and agent workflows. | covered | Trigger defined as anything that starts a workflow (shortlist candidate, new ticket/refund request, payment failure → retry workflow); event defined as a record that something happened (order created/canceled, user created/deleted); webhook defined as an HTTP request from one system to another when an event occurs (Razorpay payment success/failure/expiry calling merchant API); automation defined as reducing manual effort via chained agents (linear or branched workflows); production multi-agent systems can use event-driven architecture with HTTP communication. |
| Extra concepts taught (beyond listed subtopics) | covered | Single Responsibility Principle applied to agent design; parallelization of independent tasks vs sequential dependency constraints; agent orchestrator (workflow manager, not an agent) with orchestra analogy; HTTPS encryption vs plain HTTP; confidence scoring logic for research output; module roadmap preview (N8N, Autogen, hosting agents, LLM ops/guardrails in upcoming sessions); brief recap that prior modules covered FastAPI and foundational agent concepts. |
