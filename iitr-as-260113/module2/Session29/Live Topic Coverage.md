| Topic / subtopic | Status | Remarks |
| --- | --- | --- |
| Define the E-Commerce RAG Use Case — customer support assistant using company policy documents | Covered | Amazon-like support chatbot framed with realistic queries (returns, refunds, shipping, warranty). Extra recap tied to use case: hallucination from stale/parametric LLM knowledge and why private docs matter. |
| Identify Knowledge Sources — returns, shipping, warranty, refund policies | Covered | Named explicitly in discussion and sample data; cancellation policy added as an extra fifth sample doc type in the demo list. Extra: extended contrast of training/fine-tuning the LLM on policies vs external retrieval (cost, frequent doc updates, embedding churn). |
| Set Up a Minimal RAG Environment — libraries and reuse prior vector search | Covered | `pip` installs (OpenAI SDK, ChromaDB), project layout/API keys; references prior vector-search/session work; Sentence Transformers and Ollama noted briefly as alternatives at wrap-up. |
| Implement the Retriever Component — retrieve relevant policy content | Covered | Implemented as query embedding → similarity search on Chroma collection with configurable `top_k`. Extra: emphasized using the same embedding model for corpus and queries. |
| Inspect Retrieved Policy Content — whether results match query intent | Covered | Walkthrough of returned structure (documents list, metadata such as source/category, distances) and how to read retrieved chunks vs the question. |
| Implement the Generator Component — LLM from retrieved policy content | Covered | Generator identified as the LLM step after retrieval; API client usage and generating final natural-language answers from prompt + context. |
| Inject Retrieved Context into Prompts — policy content as grounding | Covered | Built prompts by concatenating retrieved chunks with clear delimiters and instructions to rely only on provided policy text and avoid inventing policy; role/system framing as customer-support agent. |
| Build Minimal End-to-End RAG Flow — query → retrieve → generate | Covered | Full pipeline exercised in live coding: retrieve policy chunks then call LLM with assembled prompt. Extra narrative: preprocessing path chunk → embed → store in vector DB before query-time steps. |
| Compare Responses With vs Without Retrieval | Covered | Separate “without RAG” path (LLM on query alone, no retrieval) and explicit invitation to contrast outputs against grounded answers. |
| Experiment with Retrieval Depth (Top-K) | Covered | What K means; trade-offs for K too small (missed context) vs too large (noise); “moderate” K (~3–4); experimentation and user-feedback framing; scaling K with corpus size touched qualitatively. |
| Trace Component Interaction — retriever and generator during execution | Covered | Repeated flow: query → retriever (embed + top-k search) → relevant documents → generator (prompt + LLM) → user-facing response; “relevant documents” as handoff artifact between components. |
