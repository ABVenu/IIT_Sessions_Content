| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Define and call functions using def | covered | Tea-making reusable-step analogy; DRY (Don't Repeat Yourself) principle; `def greet_user()` live demo; `print()` as a predefined function vs user-defined functions; function must be defined above the calling line; code-pointer execution flow explained. |
| Pass arguments through parameters and return results using return | covered | Parameters vs arguments with `add_two_numbers`; single and multiple parameters (`number1`, `number2`); `return` keyword taught after break; `print()` vs `return` difference; sum of first n natural numbers example using a returned value. |
| Use default parameter values when an argument is not provided | covered | Default values on `add_two_numbers` (e.g. `b=0`); honoring passed values vs using defaults; default on all parameters; additional fruit-name default demo (apple, banana, watermelon). |
| Connect multiple functions by passing one function's output as another function's input | covered | Restaurant billing capstone: `total_for_item` → `calculate_total` → `final_amount`; returned totals passed as arguments to the next function; sum-of-natural-numbers example also chains a returned sum into a formula. |
| Organize repetitive code into reusable and modular functions | partly covered | DRY principle and reuse via `add_two_numbers` called twice; restaurant billing refactored from one long script into separate functions; formal "modular programming" section not delivered as a standalone topic — only mentioned in session agenda. |
| Extra concepts taught (beyond listed subtopics) | covered | Python implicit main function and call-stack / execution flow; calling one function from another (including nested calls at any depth); function naming guidelines (name after what the function does); scope — code inside a function belongs to that function only; predefined vs user-defined functions; defining without calling produces no output; importing functions from other files briefly mentioned. |
