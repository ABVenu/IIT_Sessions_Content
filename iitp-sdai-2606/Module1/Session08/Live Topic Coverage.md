| topic/sub topic | status - (covered/not covered) | remarks |
| --- | --- | --- |
| Explain how bubble sort and selection sort work | covered | Sorting defined as arranging elements in ascending/descending order; bubble sort explained via tallest-person-in-line analogy (compare adjacent neighbors, swap if out of order, bubble largest to end each pass); plain-English algorithm steps written before coding; selection sort explained via playing-cards analogy (repeatedly pick minimum from unsorted portion); in-place swap constraint for selection sort vs two-hand card model; contrast noted — bubble sort moves larger elements right, selection sort places minimum on the left; merge sort and heapsort named but not taught. |
| Implement bubble sort and selection sort in Python | covered | `def bubble_sort(arr)` built live with nested loops (`for i in range(n-1)`, `for j in range(n-1)`), adjacent-element comparison, and swap logic; `def selection_sort(arr)` built with outer loop over positions and inner loop to find minimum index, then swap; `len()` for array length; functions called and sorted output printed; code shared via sheet at end of session. |
| Trace algorithm execution on sample arrays | covered | Bubble sort dry run on array `[5, 1, 4, 2, 8]` — step-by-step index comparisons (i, j), swaps, and state after each pass until sorted; selection sort dry run on same array with minimum/assumed-minimum/action columns tracking each iteration and resulting array state; tracing framed as a debugging skill for understanding code execution. |
| Analyze O(n²) time complexity for elementary sorting algorithms | covered | Bubble sort: nested loops analyzed — outer ~n, inner ~n → O(n²); caution that two nested loops alone do not always mean O(n²) (recap from prior session); bubble sort optimization hinted (inner loop only over unsorted portion, early termination on sorted data). Selection sort: same O(n²) reasoning via nested loops. Space complexity O(1) also discussed for both algorithms. Comparative note: bubble sort can terminate early on nearly sorted data; selection sort always scans full unsorted portion — bubble sort generally performs better in that case. |
| Extra concepts taught (beyond listed subtopics) | covered | Session agenda rationale — why learn elementary sorts when languages have built-in O(n log n) sorts (build coding intuition); previous-session recap on functions (def, call, return, parameters, reusability); Big O notation connection to prior session; definition of algorithm as a series of steps; sorting applicability to strings; sorting meaningful only for lists/collections of multiple items; built-in in-place sorting in languages briefly referenced; early-session non-technical wait/break period (~19 min of filler before lecture begins at ~19:22). |
